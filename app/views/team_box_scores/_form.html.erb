<%= form_for(@team_box_scores) do |f| %>
  <% if @team_box_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_box_score.errors.count, "error") %> prohibited this team_box_score from being saved:</h2>

      <ul>
      <% @team_box_score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
